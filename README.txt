NETRA AI WEBSITE FINAL

This package deliberately keeps the existing root files:
- logo.png
- aibg.jpg.png

Do not delete those two existing files from GitHub.

ONE-UPLOAD INSTALL:
1. In the GitHub repository root, choose Add file > Upload files.
2. Select ALL files from this folder at the same time.
3. Commit directly to main.
4. Existing files with the same names will be replaced.
5. Wait 1–5 minutes, then open https://netraai.xyz in an incognito tab.

Files included:
index.html
whitepaper.html
privacy.html
manifest.json
robots.txt
sitemap.xml
CNAME
README.txt

No hidden .github folder is required. Live market data works in the browser. The crypto-news section tries to load public RSS headlines in the browser and falls back to publisher links if the public proxy is unavailable.
